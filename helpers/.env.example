# Copy to .env (gitignored) and fill in. Sourced by submit.sh at submit time.
# Never commit .env — it holds personal info.

# Pitt email for SLURM job notifications (--mail-user).
PITT_EMAIL=your_pitt_id@pitt.edu
